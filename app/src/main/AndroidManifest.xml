<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ai.proiect_cebuc_gabriel_catalin_1079">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GrafPropActivity"></activity>
        <activity android:name=".PropertiesPropActivity" />
        <activity android:name=".RatePropActivity" />
        <activity android:name=".ProfilePropActivity" />
        <activity
            android:name=".RentPart2Activity"
            android:parentActivityName=".RentActivity" />
        <activity android:name=".RatingActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".RentActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_mata"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePagePropActivity" />
        <activity android:name=".HomePageActivity" />
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>